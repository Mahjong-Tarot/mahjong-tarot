# Daily Stand-Up — 2026-05-04 (Monday)

_Phase 1 — morning reminder log. The 09:00 compile will append the full briefing._

---

## 🌅 Morning Reminder (07:00 SGT)

**Built status lines (Step 4):**
- 🔴 Dave — `standup/individual/dave.md` (file dated `2026-04-28`, content unchanged since 28 April briefing)
- 🔴 Yon — `standup/individual/yon.md` (file dated `2026-04-29`, content unchanged since 29 April briefing)
- 🔴 Trac — `standup/individual/trac.md` (file dated `2026-04-23`, content unchanged since 23 April briefing — 11 days stale)

All three check-ins were already flagged stale on 28/29/30 April and 1 May. Today is the 5th consecutive compile with no fresh entries.

---

## 🚨 Notification Failure Log

**Lark CLI:** ❌ Failed — `Error: TAT API error: [10014] app secret invalid` on `lark-cli im +messages-send --as bot`. **5th consecutive day.** Same root cause as 28/29/30 April + 1 May: app secret for App ID `cli_a95707d3b57a5eed` is still invalid and has not been rotated.

Retry ladder per trigger §Step 5:

| # | Step | Result |
|---|------|--------|
| 1 | Initial `--text` send | ❌ `[10014] app secret invalid` |
| 2 | `lark-cli auth login --as bot` (re-auth) | ❌ Not supported — `lark-cli` v1.0.10 does not accept `--as bot` on `auth login` |
| 3 | Verify `LARK_CHAT_ID` (`echo "$LARK_CHAT_ID"`) | ✅ Set inline to `oc_e5fe68740864439744b3fb0f31f81040` (still missing from root `.env`) |
| 4 | `--markdown` instead of `--text` | ❌ Same `[10014] app secret invalid` |
| 5 | HTTP fallback via `lark-cli auth token --as bot` for tenant_access_token | ❌ Not supported — `auth token` subcommand does not exist in v1.0.10 |

The retry ladder cannot be executed end-to-end because (a) `lark-cli` v1.0.10 doesn't expose the `whoami` / `auth token` / `auth login --as bot` subcommands the trigger calls for, and (b) any direct HTTP fallback would still depend on the same rotated app secret. **Outstanding actions for Dave or Yon (carried from 28/29/30 April + 1 May):**

1. Rotate the app secret for App ID `cli_a95707d3b57a5eed` and re-authenticate `lark-cli` as bot.
2. Persist `LARK_CHAT_ID=oc_e5fe68740864439744b3fb0f31f81040` to root `.env` so Step 0 stops failing the missing-key check on every scheduled run.
3. Consider upgrading `lark-cli` (currently `1.0.10`, latest `1.0.23`) — the trigger's retry ladder assumes subcommands that exist in newer versions.

**Resend email:** Not attempted — the 7 AM trigger only calls Lark per `agents/project-manager/context/triggers/1-standup-morning.md` §Step 5.

---

## 🤖 Agent Activity Snapshot (since 2026-05-01 00:00 UTC)

Detail in `standup/individual/agents.md`. Highlights:

- **project-manager**: PRs #163 (standup-morning), #165 (standup-compile), #166 (weekly-rag) — all merged 2026-05-01.
- **web-developer**: 2 unattended `publish:` ships on 2026-05-02 — `d3b071a` (4 Feel Good Friday / Fire Horse / Decision Framework / Career Cage posts + hero images, blog index updated) and `95bf8e8` (single-file `project-status.html` dashboard, 1,016 lines).
- **other-agents**: PR #164 `Standup: 2026-05-01` (`yon-create` daily-standup-briefing automation) merged 2026-05-02 08:33 UTC. PR #170 `agents: add MailerLite Manager for OCA reactivation + Mahjong Mirror launch` (`yon-create`, branch `claude/suspicious-solomon-08f939`) **still OPEN >48h, CI green, mergeable, no reviewer assigned** — needs Dave or Yon to review.
- **writer / product-manager / image-designer**: no activity.
