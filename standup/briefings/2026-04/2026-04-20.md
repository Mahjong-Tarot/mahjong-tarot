# Daily Standup — Monday, April 20, 2026

## Yesterday
- **Lark MCP brought into Cowork** — added custom server config (`npx -y @larksuiteoapi/lark-mcp mcp -a cli_a96a84da2778deea --domain https://open.larksuite.com`); tools now exposed in Cowork sessions (im, docx, bitable, wiki, drive, contacts).
- **Lark auth scope clarified** — search APIs (`docx_builtin_search`, `wiki_v1_node_search`) failed with `user_access_token invalid or expired` and 404; tenant token works for `im_v1_chat_list`, `im_v1_message_create`, `bitable_*`, and direct `docx_v1_document_rawContent` only. Need user OAuth flow if search is required.
- **Lark server stability triaged** — server flapped twice on `"npx"` config; logs path documented (Cowork Settings → Plugins → Lark → View logs); v0.2.0 absolute-path + explicit PATH build still queued as the standing fix.
- **Cowork vs Claude Code MCP separation documented** — Lark MCP was working in Claude Code (`~/.claude.json`) but not auto-loading in Cowork; resolved by adding it through Cowork's Connectors UI.

## Today's Priorities
1. **Outreach emails & prospect list** — your stated #1, carried forward from Apr 16, 17, 18. Cold sequences + prospect targeting.
2. **Commit & push the 3 path-fix files** — `CLAUDE.md`, `context/onboarding-setup-prompt.md`, `context/setup-guide.md` still modified locally on `Yon/working-branch` since Saturday.
3. **Ship Lark v0.2.0 absolute-path build** — package and reinstall so the server stops flapping; current `npx` config is unreliable.
4. **Decide on Lark user OAuth** — if you need search/wiki across the workspace, set up the redirect URL on Lark Open Platform and get a user_access_token; otherwise scope Lark usage to messaging + direct doc reads.
5. **Check content calendar for April 20** — start at `content/content-calendar/content-calendar.md` (NOT `content/topics/INDEX.md`) for today's scheduled post.

## Heads Up
- **PR #82 status unverified** — standup PR from April 16 may still be open; confirm and merge.
- **Untracked files still sitting on branch since Saturday** — `agents/web-designer/`, `content/image-prompts.json`, `content/topics/opposites-attract-is-a-lie/`, `opposites-attract-hero-original.png`, `website/pages/blog/posts/taylor-swift-travis-kelce-wedding.jsx`, `website/public/images/blog/taylor-swift-travis-kelce-wedding.webp` — stage or gitignore.
- **`gh` auth default still resets every terminal session** — making `yon-create` permanent is the long-term fix.
