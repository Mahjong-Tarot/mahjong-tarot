# Daily Standup — Wednesday, April 29, 2026

## Yesterday
- **Dave finished the auth swap** — `fix(auth)` (`388aa0b`) wired `/signup` to **real Supabase** with **magic link** + password-eye toggle. The Clerk → Supabase migration that started Monday is now functional end-to-end on `/signup`.
- **New booking funnel shipped** — `feat(booking)` (`a68fb31`): one card-reading product with **three lengths** + new `/book-a-reading` funnel.
- **Home redesign iteration 2 landed** — `redesign(home + signup)` (`2f6e210`), real card art on deck strip (`e2bed96`), heavy red duotone dropped for clean hero (`b9b98b2`), signed-in users now redirect from `/` → `/dashboard` (`d09d394`), signup whitespace + nav-height polish (`5ba8154`, `53d1daa`).
- **PM pipeline ran clean both days** — standup-compile **#152** merged Tuesday morning; standup-morning **#154** merged ~07:06 this morning.
- **Dave filed his standup** — PR **#150** merged. `dave.md` now dated 2026-04-28 (focus: redesign + EO Forum + Linh Thai meeting).
- **Mahjong Mirror outreach moved one notch** — sender domain decided in yesterday's session: **`firepig@onlinechineseastrology.com`** (Bill's original OCA domain — best brand recognition for the dormant list). Next step is MailerLite domain authentication.

## Today's Priorities
1. **Merge PR #151** — yesterday's standup briefing is still open from Tuesday morning. Clean it up before today's PR lands so the standup branch series stays linear.
2. **MailerLite domain auth for `onlinechineseastrology.com`** — biggest unblocker for the pilot send. In MailerLite: Account settings → Domains → Add domain → grab the SPF + DKIM records → get them into DNS for `onlinechineseastrology.com` (Bill or whoever holds DNS) → verify. Once green, sender = `firepig@onlinechineseastrology.com`, display = "Bill / The Firepig".
3. **Close the v1.3 gates** — still blocking Sequence D: (a) physical mailing address for CAN-SPAM footer, (b) EU-resident handling decision (Bill's source has no metadata to flag them).
4. **Review PR #153** — k2's `fix: improve p2 discovery` opened Tuesday afternoon, no description. Sitting open ~17h.
5. **Sync local main** — local is **1 ahead, 5 behind** `origin/main`. Pull and figure out what your single local commit is before today's standup PR opens.

## Heads Up
- **Lark bot auth — day 5 of failures.** This morning's `agents.md` still flags `[10014] app secret invalid` for App ID `cli_a95707d3b57a5eed`. Recurring across all automated runs; team is not getting Lark pings, only file markers. Trac/Dave still need to rotate the secret + persist `LARK_CHAT_ID` in `.env.local`. `RESEND_API_KEY` also still missing.
- **Trac's standup is a week stale** — still dated **2026-04-23** ("bootstrap cleanup"). Yours and Dave's are fresh.
- **Today's auto-compile hasn't fired yet** — only the morning marker (PR #154) is in. If standup-compile doesn't run by ~10 AM ICT, today's team-wide briefing won't materialize (same pattern flagged yesterday — Tuesday's compile eventually ran clean, so likely fine).
- **`yon.md` still says "MailChimp"** — flagged yesterday, still not updated. Live work is on MailerLite.
- **Untracked pile on local main is unchanged** — same 7 items still sitting (`.brv/`, `agents/web-designer/`, `content/image-prompts.json`, opposites-attract content + hero, taylor-swift `.jsx` + `.webp`). Stage, gitignore, or delete.
